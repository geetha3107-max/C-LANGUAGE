#include<stdio.h>
void main()
{
    int a,b;
    printf("enter a,b values:");
    scanf("%d%d",&a,&b);
    int c=(a>b)? a:b;
    int d=c;
    while(1){
    if(c%a==0 && c%b==0){
        break;
    }
    else{
        c=c+d;
    }
    printf("LCM is %d",c);
}
}
